# News providers V2: implemented connectors and operating limits

Reviewed and tested on 9 September 2026. This document describes the adapters in `src/v2/providers/`; the application architecture and deployment checks are documented separately. The implementation supports full reading of selected official releases and customer-configured licensed feeds. It does not turn a publisher's consumer subscription into an API or full-text licence.

## Implemented source catalogue

| Connector | Real upstream interface | What the V2 reader receives | Access and observed status |
| --- | --- | --- | --- |
| Federal Reserve Board | `GET https://www.federalreserve.gov/feeds/press_all.xml`, followed by selected `/newsevents/pressreleases/*.htm` URLs on the same host | Official headline, publication date, RSS description, plain-text release body, original link and attribution. Policy/regulatory material, not a general company-news wire. | No key or source fee. RSS and a selected release returned HTTP 200 in a bounded live check. The [Board disclaimer](https://www.federalreserve.gov/disclaimer.htm) allows reuse of Board information unless otherwise indicated; third-party material and seals are excluded. |
| European Central Bank | `GET https://www.ecb.europa.eu/rss/press.html`, followed only by `/press/pr/date/YYYY/html/ecb.*.en.html` | Selected English institutional press releases, their publication dates, plain-text bodies and original links. Authored speeches, research papers and PDFs are excluded by the URL policy. | No key or source fee. RSS and a selected release returned HTTP 200. The [ECB disclaimer](https://www.ecb.europa.eu/services/using-our-site/disclaimer/html/index.en.html) requires accurate reproduction, source acknowledgement and disclosure of changes. The reader says the source is free on the ECB website and formatting is simplified. A paid product must also disclose free source availability before payment and each access; that checkout requirement is outside this local reader. |
| SEC EDGAR | Latest filing Atom feed at `https://www.sec.gov/cgi-bin/browse-edgar?action=getcurrent&count=100&output=atom`; `https://data.sec.gov/submissions/CIK##########.json`; exact filing documents under `https://www.sec.gov/Archives/edgar/data/` | Issuer disclosures for supported forms, accession ID, filing acceptance timestamp, CIK and available ticker/exchange evidence. Body text is fetched from the exact primary document when reachable. | No key or source fee. The [SEC reuse guidance](https://www.sec.gov/about/webmaster-frequently-asked-questions) covers public EDGAR filing content. **Latest-feed and targeted-submissions live checks returned HTTP 403 from this environment.** The connector reports the block; it does not claim successful live filing ingestion. |
| Benzinga | `GET https://api.benzinga.com/api/v2/news` and `/api/v2/news-removed`, with provider token | Headline, teaser, publication/update dates, stock associations and removal events. Full plain-text body only when the workspace explicitly enables licensed full-text display and the upstream response actually contains a body. | Customer API credentials and the applicable business/display agreement are required. API mechanics are implemented and fixture-tested; no paid upstream request or entitlement test has been performed. [News API](https://docs.benzinga.com/api-reference/news-api/get-news-items), [removal API](https://docs.benzinga.com/api-reference/news-api/get-removed-news). |
| Marketaux | `GET https://api.marketaux.com/v1/news/all`, with `api_token`, a bounded publication window and pagination | English publisher-attributed descriptions/snippets, original links, publication dates and entity identifiers. **No full article body.** | Customer API credentials and applicable business-display permission are required. A paid subscription does not supply full articles: the [FAQ](https://www.marketaux.com/faq) explicitly describes snippets and original links. The [terms](https://www.marketaux.com/tos) require checking the commercial-use scope. Implemented and fixture-tested; paid upstream delivery untested. |
| Finnhub | `GET https://finnhub.io/api/v1/news?category=general`, or bounded `/api/v1/company-news` requests for at most five explicitly configured symbols | Publisher summaries, links and source publication timestamps; no full article body. General-market mode does not infer company associations. | Customer API credentials and applicable business-use permission are required. [Finnhub terms](https://finnhub.io/terms-of-service) restrict personal plans and require written approval for business use. Company-news coverage is described as North American in the [API documentation](https://finnhub.io/docs/api/company-news). Implemented and fixture-tested; paid upstream delivery untested. |

The source labels remain distinct: official-policy release, issuer disclosure, licensed news and publisher summary. No adapter imports images: an image URL or a text licence is not treated as proof of image-display rights. The stored body is plain text; the implementation does not reproduce page navigation, advertisements, executable HTML or arbitrary publisher pages.

The live sample illustrates freshness rather than promising a real-time news wire: the first Fed release was published on 4 September 2026, and the first eligible ECB institutional release was published on 21 August 2026. More recent ECB feed items were authored speeches excluded by the reuse policy. Their publication dates are preserved; fetching today does not make an older release new.

## Permission and payment

For the selected Fed and ECB materials, the implementation relies on their published reuse conditions and attribution, without a new paid API agreement. SEC provides a similar free primary-source route when reachable. These sources provide useful original announcements and disclosures, but do not replace global financial journalism or prove coverage of every portfolio company.

The three commercial connectors stay disabled until an administrator supplies provider-issued API credentials and confirms that the customer's agreement covers display in this workspace. This is an attestation recorded by the product, not automated legal verification. Benzinga full-text display has a separate switch. A consumer login, an API key, a vendor free tier, or a successful HTTP response alone does not establish employee/customer display, full-body retention, image, redistribution or model-training rights. The adapters do not send source bodies to an AI service.

For an internal company deployment, confirm employee access and any retention restrictions. For customer-facing deployment, obtain the relevant redistribution/display rights. Connecting different customers' subscriptions requires separate tenant credentials, storage and entitlement isolation; the present connector configuration is scoped to one workspace and is not a multi-tenant licensing system.

Benzinga is the implemented candidate for licensed in-platform journalism. Its [AWS offering](https://aws.amazon.com/marketplace/pp/prodview-xwgvhwowjmw3g) and linked agreement distinguish the licensed service and permitted users; the customer's actual order and licence govern display and redistribution. An applicable full-text price and quota have not been quoted or purchased. Marketaux and Finnhub are summary connectors even if a customer pays for their API plan. Vendor plans and prices must be checked against the customer's required scope; the local request budget below is not a vendor price or entitlement.

## Checkpoints, freshness and request budgets

Every actual upstream HTTP request reserves the worker's persistent PostgreSQL quota before it is sent. This includes feed downloads, release-body downloads and SEC mapping/document requests. Local defaults are 30 requests/minute for each official source, 20 for Benzinga, and 10 for each summary vendor, further bounded by the configured daily quota. These are application limits, not statements about purchased vendor quotas. SEC also has a shared per-host-family delay of 550 ms, below two requests/second, stricter than its published [10 requests/second maximum](https://www.sec.gov/search-filings/edgar-application-programming-interfaces).

The worker normally checks free sources every 15 minutes and enabled commercial sources every five minutes. Cadence is configurable and does not guarantee publication-to-screen latency: provider delivery, quota, queueing and outages can delay updates.

| Connector | Persisted continuation | What completion means; important limits |
| --- | --- | --- |
| Fed / ECB | Up to 100 eligible feed entries, fingerprints of title/description/date/category/URL, and a daily full-scan timestamp. Each batch fetches at most 10 bodies. | New or changed feed entries fetch their bodies at the feed-check cadence. An unchanged feed causes one RSS request and no body downloads. Current-feed bodies are rechecked once per 24 hours to detect changes that do not alter RSS metadata, subject to quota and availability. Pending rescans resume after a bounded job. Releases no longer in RSS are not monitored for corrections or inferred to be withdrawn. |
| SEC | Fixed pending filing metadata and the current CIK index. At most five documents per batch. Optional targeted mode accepts at most 10 CIKs and selects the latest 10 supported disclosures for each. | A bounded current-feed or targeted snapshot, not a full filings archive. Forms are 8-K/8-K/A, 6-K/6-K/A, 10-Q/10-Q/A, 10-K/10-K/A, 20-F and 40-F. Feed mode resolves the exact primary document through submissions data, caching that mapping within the batch. SEC is currently blocked here, so no claim of live completeness is made. |
| Benzinga | Fixed initial update window, page offset and a news/removal phase. News uses `updatedSince` and `sort=updated:asc`; removals use the documented removal endpoint. | Both phases must finish before the stable watermark advances. A five-minute overlap catches repeated boundary updates, with source IDs used for deduplication. Offset pagination is not a transactional snapshot or a lossless stream. The removal offset ceiling stops the job rather than silently skipping data. |
| Marketaux | Fixed publication window and page offset; continuation uses the actual `meta.limit` returned for the customer's plan. | Completion exhausts the bounded result window. The adapter stops at the documented 20,000-result ceiling rather than advancing a false watermark. Five-minute overlap reduces late-arrival gaps; old articles updated outside that publication overlap may be missed. There is no implemented removal stream. |
| Finnhub | General-news ID watermark, or the current explicitly selected symbol plus pending items. | General mode is a bounded snapshot. Targeted mode covers at most five symbols with overlapping dates, not a crawl of all portfolio companies. No general correction/removal guarantee is inferred. |

For today's 20-item Fed feed, an unchanged 24-hour operating cycle at 15-minute cadence is about 96 RSS requests plus 20 daily body checks, plus any new/changed entries and exceptional retries. This avoids downloading all 20 bodies on every poll. If a daily correction check fails for an unchanged previously imported body, the existing article is preserved rather than erased. Older checkpoints without fingerprints intentionally trigger one fresh body scan when migrated.

The backend keeps a resume cursor for incomplete jobs and advances the stable provider checkpoint only for a completed window. A default job processes at most three adapter batches and becomes partial if more work remains. An authentication/access failure is reported with its sanitized HTTP status and pauses automatic retries until settings are saved or the operator requests a manual fetch. No VPN, alternative identity, request-header rotation or paywall workaround is implemented.

## Company identity and scale

The default strategy ingests a provider feed once, then associates returned entities with the company directory. It does not fetch the same global feed separately for 1,500, 10,000 or 50,000 companies. A five-minute per-company polling design would require approximately 432,000, 2.88 million or 14.4 million requests/day respectively, before pagination; those are arithmetic workload estimates, not vendor capacity claims.

Marketaux observations preserve name, ticker, exchange, country and a provider identifier. Benzinga's documented `stocks[].name` field is a ticker, **not a company name**; the adapter maps it to ticker and preserves any supplied exchange and ISIN. SEC preserves the CIK, accession, and available issuer name/listing evidence. The strict directory resolver can use supplied exchange/name evidence and leaves weak or ambiguous matches unresolved. It does not assume every symbol belongs to a US listing.

Finnhub's current targeted adapter records the requested ticker as query provenance. Its company-news response does not itself establish an exchange or canonical company ID, so the bare ticker is not enough for a verified directory association. A verified symbol-to-listing mapping is needed before relying on Finnhub company-news in portfolio filters. Official macro releases are not assigned to companies based on incidental text mentions. No connector test establishes complete coverage of the 50,000-company target.

For the next licensed rollout, obtain a feed entitlement with the required geographic/company scope, validate exchange-qualified entity mappings on a representative portfolio, confirm permitted users and article retention, and measure source latency and gaps with that actual account. LSEG/Reuters and Dow Jones remain separate enterprise procurement candidates, not implemented adapters. [LSEG's news product overview](https://developers.lseg.com/en/product/news/overview) distinguishes programmatic and display usage, so an analytics feed should not be assumed to include a human article reader.

## Safety and verification

The fetcher uses a fixed HTTPS host/path allowlist, denies redirects, enforces a 15-second request timeout and streamed byte limits, and calls the durable request-budget callback for every real network request. Official body HTML is converted to paragraphs of plain text. Unknown layouts fail to summary/link mode rather than importing a login or error page. SEC uses an honest project User-Agent with `https://github.com/1nsar/investin-com-news-service`, not an invented contact address.

Provider keys remain server-side and are never returned by the adapters; reflected literal or URL-encoded API credentials are redacted from returned data. API URLs containing keys and raw response bodies are not included in operational error messages. Paid connection tests do not create subscriptions, trades, purchases or vendor accounts.

Validation completed for these adapters: **20 focused tests passed**, covering real source URL boundaries, heading-versus-body extraction, unavailable content, persistent request accounting, feed continuation, metadata changes/daily rescans, SEC exact-document mapping, Marketaux actual plan page size, Benzinga full-text gating/removals, update-versus-correction labels, Finnhub symbol continuation and credential redaction. A Benzinga publication/update timestamp difference is displayed as an update; an editorial correction label requires an explicit boolean source marker. The service TypeScript check passed. Bounded live requests verified Fed and ECB feeds and selected bodies; SEC access failed with HTTP 403. Commercial payload tests use fixtures, and do not verify the customer's future API entitlement or actual commercial feed delivery.
